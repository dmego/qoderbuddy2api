# 2api 项目 Agent 执行规则

本文件是 `2api` 仓库的项目级执行规则。用户在当前会话中的明确要求优先于本文件；本文件优先于全局个人规则和通用 skill 默认流程。

## 1. 核心原则：以大型交付任务为单位推进

- 默认采用满足质量要求的最短路径，不按 Superpowers 的完整流程拆解每一个小步骤。
- 一个大型交付任务应形成完整、可使用的业务能力或技术闭环，可以同时修改多个模块和较多文件。
- 不要为了套用 skill、TDD、review 或 commit 流程，把一个完整任务继续拆成大量小 task。
- 小函数、小组件、小接口、小测试和局部重构默认属于当前大型任务内部实现，不单独建立 plan、review 或验收循环。
- 除非用户明确要求，工作过程中不要因非阻断问题停下来等待确认；继续推进到大型任务完成、真实阻塞或集成验收点。

## 2. Superpowers 使用方式

- Superpowers 是可选的工程辅助，不是本项目逐步骤强制门禁。
- 默认不为每个小 task 依次执行 `brainstorming -> writing-plans -> TDD -> requesting-code-review -> verification-before-completion`。
- 大型、方向不清或高风险任务可以先做一次简短设计和执行计划；目标清楚后直接实现。
- `systematic-debugging` 只用于根因不明确的真实故障，不用于已经明确修复方案的普通问题。
- TDD 仅用于高风险共享逻辑、并发、认证、凭据、持久化和重要回归；普通 UI、文案、映射和局部适配不强制先写 RED。
- Review 以集成后的大型任务或并行 Wave 为单位，不为每个小改动反复启动独立 reviewer。
- 只有可复现的 Critical / Important 问题阻止合并；Minor 默认记录到后续清理任务，不进入反复 review-fix-review 循环。

## 3. 并行开发与任务粒度

- 可以自然拆成 2 到 4 个边界清晰的大型任务时，优先使用多个子代理和独立 worktree 并行开发。
- 每个子代理负责完整领域或完整垂直能力，不分派只有几个函数或单个样式调整的微任务。
- 并行任务必须提前明确主要文件所有权；共享入口、schema、根配置、路由、shared types 和 lockfile 由集成人统一收口。
- 子代理完成大型任务后，只需运行一次任务级聚合验证并提交；不要在每个内部步骤重复执行全套测试。
- 子代理报告应包含：提交 SHA、主要改动、实际验证结果、已知风险和与其他任务的契约假设。

## 4. 合并优先、集中解决冲突

- 大型任务达到可运行状态并通过基本任务级验证后，应合并到集成分支；不要在子分支上进行多轮重型审查后才允许合并。
- 按依赖顺序逐个 cherry-pick / merge 大型任务，集中处理共享文件和构建产物冲突。
- 前后端并行任务的 API 字段、状态枚举和错误码在集成阶段统一对齐，并增加少量跨模块契约测试。
- 前端 `dist` 冲突不逐文件人工保留旧 hash 产物；优先合并源码，最后统一重新构建生成最终产物。
- schema、repository composition、应用入口和运行时 lifecycle 冲突由集成人统一处理，不让多个任务分支重复修改和反复返工。

## 5. 验证策略

- 实现过程中只运行能快速定位问题的定向测试；不要求每个小修改都测试。
- 每个大型任务完成后运行一次任务级聚合验证。
- 多个大型任务合并后运行一次 Wave 级完整门禁，而不是对每个 cherry-pick 重复跑完整套件。
- Wave 级门禁通常包括：
  - Python full suite；
  - Ruff 和 compileall；
  - 前端 test、typecheck、lint、build；
  - 必要的真实双进程、迁移、重启恢复或浏览器 smoke；
  - `git diff --check`。
- 没有实际验证证据不能声称通过，但允许明确标记“代码完成，等待 Wave 集成验证”。
- 外部账号、真实 Windows、真实上游或其他当前环境无法验证的行为必须如实标记，不为此阻塞其他可完成工作。

## 6. Review 策略

- 默认在大型任务合并后或一个并行 Wave 合并后进行一次集中 review。
- 认证、密钥、并发、流式响应、数据库迁移等高风险区域可以在合并前做一次定向 review，但不要默认进行多轮独立复审。
- Review 发现问题后：
  - Critical / Important：集中形成一个 review-fix 批次，修复并做定向回归；
  - Minor：记录到清理清单，除非修复极小且不会打断主线，否则不阻塞当前任务；
  - 建议项：不自动扩展当前范围。
- 一个 review-fix 批次完成后，由集成人检查代码和测试结果即可；只有安全边界仍存在疑问时才再派独立复审。

## 7. Commit 与进度规则

- commit 以大型任务或一次集中 review-fix 批次为单位，不为每个小步骤提交。
- commit message 使用：`<type>(scope): <summary>`，英文 summary 不超过 50 个字符。
- 合并冲突处理、跨任务契约对齐和最终构建产物可以形成独立 integration commit。
- 进度按大型交付任务统计，并同时区分：
  - 已编码；
  - 已提交；
  - 已合并；
  - 已通过 Wave 门禁。
- 不用小测试数量、修改文件数量或子步骤数量虚增完成度。
- 长时间运行时应定期报告：完成的大型任务、正在进行的任务、剩余任务、当前阻塞和估算百分比。

## 8. 本项目不可降低的质量与安全边界

- Control Plane 与 Proxy Worker 的进程、认证和凭据边界必须保持清晰。
- Worker 默认仅监听 loopback，不直接读取 SQLite、凭据主密钥或原始管理密钥。
- 原始 Token、Cookie、API Key、Authorization、上游响应正文和 prompt/completion 不得写入日志、审计、SQLite、前端持久化存储或提交记录。
- 流式响应只允许在第一个下游 chunk 之前 refresh / failover；输出后禁止跨账号重试。
- 数据库 mutation、migration、备份恢复和凭据轮换必须保留数据安全与失败恢复语义。
- 前端必须是功能完整的管理台，不以“简约演示页”代替真实管理能力；主要功能应能在 UI 中配置、执行并观察结果。
- 最终任务必须清理废弃代码、旧前端构建、无用兼容层和过时文档，并同步实际部署、配置和运维说明。

## 9. 环境与命令

- 本机 shell 命令使用 `rtk` 前缀。
- 前端依赖安装使用阿里镜像：`--registry=https://registry.npmmirror.com`。
- 保留用户已有改动，不使用 `git reset --hard`、破坏性 checkout 或未经授权的删除。
- 未经用户明确要求，不 push、不创建 PR、不改写远程历史。

